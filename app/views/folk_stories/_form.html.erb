<%= form_for(@folk_story) do |f| %>
  <% if @folk_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@folk_story.errors.count, "error") %> prohibited this folk_story from being saved:</h2>

      <ul>
      <% @folk_story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
